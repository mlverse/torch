# Adds the 'jit_scalar' class to the input

Allows disambiguating length 1 vectors from scalars when passing them to
the jit.

## Usage

``` r
jit_scalar(x)
```

## Arguments

- x:

  a length 1 R vector.

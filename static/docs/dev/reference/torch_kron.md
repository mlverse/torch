# Kronecker product

Computes the Kronecker product of `self` and `other`.

## Usage

``` r
torch_kron(self, other)
```

## Arguments

- self:

  (`Tensor`) input Tensor

- other:

  (`Tensor`) other tensor.

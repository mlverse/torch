# A simple exported version of install_path Returns the torch installation path.

A simple exported version of install_path Returns the torch installation
path.

## Usage

``` r
torch_install_path(check_writable = FALSE)
```

## Arguments

- check_writable:

  If `TRUE`, checks if the installation path is writable.

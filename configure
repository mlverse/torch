#!/usr/bin/env sh

PKG_PATH=$(pwd)

if ! test -z $DEVTOOLS_LOAD; then
  PKG_PATH="$PKG_PATH/inst"
fi

if [[ "$BUILD_LANTERN" != true ]] && [[ "$BUILD_LANTERN" != 1 ]]; then
  BUILD_LANTERN=""
fi

cd src
sed -e "s|@PKG_PATH@|$PKG_PATH|" -e "s|@BUILD_LANTERN@|$BUILD_LANTERN|" Makevars.in > Makevars
